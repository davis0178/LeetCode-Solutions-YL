```python
class Solution:
    def minimumDifference(self, nums: List[int], k: int) -> int:
        nums.sort()
        return min(nums[i + k - 1] - nums[i] for i in range(len(nums) - k + 1))
```
最优的方案是连续选择有序数组中的 $k$ 个元素。首先对数组升序排序，用大小固定的滑动窗口遍历

#数组 #排序 #滑动窗口