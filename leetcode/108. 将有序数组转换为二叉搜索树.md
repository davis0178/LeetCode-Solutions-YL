```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:
        if not nums:
            return
        
        m = len(nums) // 2
        left = self.sortedArrayToBST(nums[:m])
        right = self.sortedArrayToBST(nums[m+1:])

        return TreeNode(nums[m], left, right)
```

根节点就是有序数组的中间值 `len(nums) // 2`
答案可能不是唯一的，如果 n 是偶数，可以取数组正中间左边或右边的那个树作为根节点的值
然后使用递归求解：把根节点左边的数组转换成一棵平衡二叉搜索树，作为答案的左儿子。把根节点右边的数组转换成一棵平衡二叉搜索树，作为答案的右儿子。

#树 #二叉搜索树 #数组 #分治 #二叉树